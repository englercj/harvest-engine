// Copyright Chad Engler

plugin he.make version="0.1.0" license="UNLICENSED" {
    authors { "Chad Engler" email=englercj@live.com }

    module Harvest.Kdl kind=lib_shared group="engine/make" project="${module.name}/${module.name}.csproj"
    module Harvest.Make kind=app_console group="engine/make" project="${module.name}/${module.name}.csproj"
    module Harvest.Make.Projects kind=lib_shared group="engine/make" project="${module.name}/${module.name}.csproj" hemake_ext=#true
}
