# Copyright Chad Engler

# Harvest Engine root import plugin
# This plugin acts as an easy way to import the entire engine.

id = "he.engine-root"
import_plugins = ["contrib/*", "plugins/*"]

[[modules]]
    name = "he_engine_root"
    type = "header"
    group = "engine/libs"
    files = [".editorconfig"]
